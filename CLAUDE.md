
# CLAUDE.md

本项目是一个面向生物医学科研分析的 Research Agent，用于调度和执行已有的科研分析 Skills。

开始开发前，请先阅读：

- `SPEC.md` —— 产品需求、核心流程和预期行为

## 开发原则

- 现有 Skills 是科研分析流程的主要实现，不要无必要重写分析 pipeline。
- Agent 只负责调度：选择 Skill、执行 Skill、检查输出、和用户交互。
- 优先实现最小可用版本。
- 优先复用已有代码，再考虑新增抽象。
- 不要为了未来可能出现的需求过度设计。
- 所有分析结果尽量保存在当前 Project workspace 中。
- 如果执行失败，必须展示真实错误，不要伪装成成功。

## 当前优先级

先把 `SPEC.md` 中定义的核心端到端流程完整跑通。

除非明确要求，否则不要提前增加无关基础设施或高级功能。
